dependencies:
  pre:
    - chmod 777 scripts/install_geckodriver.sh
    - ./scripts/install_geckodriver.sh

test:
  pre:
    - coverage erase
  override:
    - coverage run manage.py test
  post:
    - coveralls
    - coverage html -d $CIRCLE_ARTIFACTS
